<Window x:Class="Gx12.Launcher.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Gx12.Launcher.Wpf.Controls"
        xmlns:models="clr-namespace:Gx12.Launcher.Wpf.Models"
        Title="Asukaflight"
        Width="1360"
        Height="850"
        MinWidth="1080"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBaseBrush}"
        controls:OptionToolTips.IsEnabled="True">
    <Window.Resources>
        <Style x:Key="PanelStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource BgSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="0" />
        </Style>

        <Style x:Key="RaisedBlockStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource BgRaisedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="TextWrapping" Value="NoWrap" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,3,0,0" />
            <Setter Property="LineHeight" Value="20" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="TextWrapping" Value="NoWrap" />
        </Style>

        <Style x:Key="EditorTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="MinWidth" Value="92" />
            <Setter Property="Margin" Value="0,0,10,8" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="EditorCheckBoxStyle" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,0,14,8" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="ToolTipService.ShowDuration" Value="30000" />
        </Style>

        <Style x:Key="CompactButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinHeight" Value="29" />
        </Style>

        <Style x:Key="EditorComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource BgRaisedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="MinHeight" Value="31" />
            <Setter Property="Margin" Value="0,0,10,8" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="ToolTipService.ShowDuration" Value="30000" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1"
                                          ClickMode="Press"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ToolTip="{TemplateBinding ToolTip}"
                                          ToolTipService.ShowOnDisabled="True"
                                          ToolTipService.ShowDuration="30000">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="Chrome"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{StaticResource TextMutedBrush}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,10,0" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="Chrome" Property="Background" Value="#15191C" />
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#252B2F" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <TextBlock Text="{TemplateBinding SelectionBoxItem}"
                                       Foreground="{TemplateBinding Foreground}"
                                       Margin="10,0,28,0"
                                       VerticalAlignment="Center"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       IsHitTestVisible="False" />

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource BgRaisedBrush}"
                                        BorderBrush="{StaticResource AccentBrush}"
                                        BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#667176" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource BgRaisedBrush}" />
            <Setter Property="Padding" Value="9,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemChrome"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="#263036" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#667176" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveStateBadgeStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="#203330" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="9,5" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Editor.SaveStateKind}" Value="Dirty">
                    <Setter Property="Background" Value="#332C1D" />
                    <Setter Property="BorderBrush" Value="{StaticResource WarnBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Editor.SaveStateKind}" Value="Invalid">
                    <Setter Property="Background" Value="#331F21" />
                    <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RuntimeStateBadgeStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="#242A2E" />
            <Setter Property="BorderBrush" Value="#3A4247" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="10" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding RuntimeStateKind}" Value="Accent">
                    <Setter Property="Background" Value="#203330" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding RuntimeStateKind}" Value="Warn">
                    <Setter Property="Background" Value="#332C1D" />
                    <Setter Property="BorderBrush" Value="{StaticResource WarnBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BadgeBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="#242A2E" />
            <Setter Property="BorderBrush" Value="#3A4247" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="Margin" Value="0,0,5,5" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Kind}" Value="Default">
                    <Setter Property="Background" Value="#24332F" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Kind}" Value="Accent">
                    <Setter Property="Background" Value="#203330" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Kind}" Value="Info">
                    <Setter Property="Background" Value="#202B36" />
                    <Setter Property="BorderBrush" Value="{StaticResource InfoBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Kind}" Value="Warn">
                    <Setter Property="Background" Value="#332C1D" />
                    <Setter Property="BorderBrush" Value="{StaticResource WarnBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Kind}" Value="Danger">
                    <Setter Property="Background" Value="#331F21" />
                    <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="BadgeTemplate" DataType="{x:Type models:ProfileBadge}">
            <Border Style="{StaticResource BadgeBorderStyle}">
                <TextBlock Text="{Binding Label}"
                           FontSize="11"
                           Foreground="{StaticResource TextBrush}"
                           TextWrapping="NoWrap" />
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="ReleaseCheckTemplate" DataType="{x:Type models:ReleaseCheckItem}">
            <Border Background="#1B2023"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="8"
                    Margin="0,0,7,7"
                    MinWidth="210">
                <DockPanel LastChildFill="True">
                    <Border DockPanel.Dock="Left"
                            Style="{StaticResource BadgeBorderStyle}"
                            MinWidth="74"
                            Margin="0,0,8,0">
                        <TextBlock Text="{Binding Status}"
                                   FontSize="11"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   HorizontalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock Text="{Binding Label}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Detail}"
                                   Style="{StaticResource MutedTextStyle}"
                                   FontSize="11"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </DockPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="248" MinWidth="232" />
            <ColumnDefinition Width="*" MinWidth="780" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Style="{StaticResource PanelStyle}"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="12">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Profile Library"
                               FontSize="16"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding ProfileCountSummary}"
                               Style="{StaticResource MutedTextStyle}"
                               Margin="0,2,0,10" />

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="32" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Search profile names, files, modes, sources, and badges." />
                        <Button Grid.Column="1"
                                Content="X"
                                Command="{Binding ClearSearchCommand}"
                                Margin="6,0,0,0"
                                Padding="0"
                                ToolTip="Clear search." />
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <UniformGrid Grid.Row="0" Columns="2">
                            <Button Content="Clone"
                                    Command="{Binding CloneProfileCommand}"
                                    Margin="0,0,5,0"
                                    ToolTip="Create a line-preserving copy of the selected profile." />
                            <Button Content="Refresh"
                                    Command="{Binding RefreshCommand}"
                                    Margin="5,0,0,0" />
                        </UniformGrid>
                        <Button Grid.Row="1"
                                Content="Change folder"
                                Command="{Binding ChangeFolderCommand}"
                                Margin="0,6,0,0"
                                ToolTip="Choose the profile folder used by the launcher." />
                    </Grid>
                </StackPanel>

                <ListBox ItemsSource="{Binding ProfilesView}"
                         SelectedItem="{Binding SelectedProfile, Mode=TwoWay}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ProfileSummary}">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Text="{Binding DisplayName}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap" />
                                <TextBlock Grid.Row="1"
                                           Text="{Binding FileName}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,2,0,7"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap" />
                                <ItemsControl Grid.Row="2"
                                              ItemsSource="{Binding Badges}"
                                              ItemTemplate="{StaticResource BadgeTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                                <TextBlock Grid.Row="3"
                                           Text="{Binding SafetySummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,2,0,0"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1"
                Background="{StaticResource BgBaseBrush}">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Margin="20,16,20,12">
                    <DockPanel Margin="0,0,0,18">
                        <Border DockPanel.Dock="Right"
                                Style="{StaticResource SaveStateBadgeStyle}"
                                Margin="16,0,0,0"
                                VerticalAlignment="Top">
                            <TextBlock Text="{Binding Editor.SaveStateText}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="310" />
                        </Border>
                        <StackPanel DockPanel.Dock="Left">
                            <TextBlock Text="{Binding Editor.ProfileName}"
                                       FontSize="24"
                                       FontWeight="SemiBold"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding SelectedProfileSubheader}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,4,0,0"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DockPanel>

                    <ItemsControl ItemsSource="{Binding SelectedProfile.Badges}"
                                  ItemTemplate="{StaticResource BadgeTemplate}"
                                  Margin="0,0,0,12">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource RaisedBlockStyle}" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="Mode" Style="{StaticResource MetricLabelStyle}" />
                                <TextBlock Text="{Binding SelectedProfile.ModeLabel}" Style="{StaticResource MetricValueStyle}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource RaisedBlockStyle}" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="Frame rate" Style="{StaticResource MetricLabelStyle}" />
                                <TextBlock Text="{Binding SelectedProfile.FrameRateHz}" Style="{StaticResource MetricValueStyle}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource RaisedBlockStyle}" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="Right stick" Style="{StaticResource MetricLabelStyle}" />
                                <TextBlock Text="{Binding SelectedProfile.RightSourceLabel}" Style="{StaticResource MetricValueStyle}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="3" Style="{StaticResource RaisedBlockStyle}" Margin="0">
                            <StackPanel>
                                <TextBlock Text="Left stick" Style="{StaticResource MetricLabelStyle}" />
                                <TextBlock Text="{Binding SelectedProfile.LeftSourceLabel}" Style="{StaticResource MetricValueStyle}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="260" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Grid.Column="0"
                                Style="{StaticResource RuntimeStateBadgeStyle}"
                                Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="{Binding RuntimeStateLabel}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding RuntimeStateDetail}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,4,0,0"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="0"
                                Grid.Column="1"
                                Background="{StaticResource BgSurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Text="{Binding RuntimeStatusText}"
                                           Style="{StaticResource MutedTextStyle}"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Row="1"
                                           Text="{Binding TrainerCommandLine}"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,6,0,0"
                                           ToolTip="{Binding ConsoleScriptPath}" />
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1"
                              Grid.Column="0"
                              Grid.ColumnSpan="2"
                              Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <WrapPanel Grid.Column="0"
                                       VerticalAlignment="Center">
                                <Button Content="Set default"
                                        Command="{Binding SetDefaultCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="0,0,6,6" />
                                <Button Content="Copy path"
                                        Command="{Binding CopyPathCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="0,0,6,6" />
                                <Button Content="Copy command"
                                        Command="{Binding CopyTrainerCommandLineCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="0,0,6,6" />
                                <Button Content="Copy script"
                                        Command="{Binding CopyConsoleScriptPathCommand}"
                                        Style="{StaticResource CompactButtonStyle}"
                                        Margin="0,0,6,6" />
                            </WrapPanel>

                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Top">
                                <Button Content="{Binding StartTrainerButtonText}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding StartCompositeTrainerCommand}"
                                        Padding="16,8"
                                        Margin="10,0,8,6"
                                        ToolTip="{Binding TrainerCommandLine}" />
                                <Button Content="{Binding StopRunButtonText}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Command="{Binding StopRunCommand}"
                                        Padding="16,8"
                                        Margin="0,0,0,6" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </StackPanel>

                <TabControl x:Name="WorkspaceTabs" Margin="20,0,20,20">
                    <TabItem Header="Dashboard">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Dashboard" Style="{StaticResource SectionTitleStyle}" />

                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Profile file" Style="{StaticResource MutedTextStyle}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedProfile.FileName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Default marker" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DefaultProfileFileName}" Margin="0,8,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Safety keys" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedProfile.SafetySummary}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Gains" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedProfile.GainSummary}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Right tuning" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedProfile.RightTuningSummary}" Margin="0,8,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Left yaw" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedProfile.LeftYawTuningSummary}" Margin="0,8,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Reticle aim" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SelectedProfile.MouseAimTuningSummary}" Margin="0,8,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Last write" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding SelectedProfile.LastWriteSummary}" Margin="0,8,0,0" />
                                </Grid>
                            </Border>

                            <TextBlock Text="Control Sources" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Right device" Style="{StaticResource MutedTextStyle}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedProfile.DeviceSummary}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Keyboard source" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedProfile.KeyboardInputSource}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Profile folder" Style="{StaticResource MutedTextStyle}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ProfileDirectory}" Margin="0,8,0,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Health" Style="{StaticResource SectionTitleStyle}" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <StackPanel>
                                    <TextBlock Text="{Binding ExeStatus}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding ExePath}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,5,0,0"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="NoWrap" />
                                </StackPanel>
                            </Border>

                            <TextBlock Text="Command Line" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <TextBlock Text="{Binding TrainerCommandLine}"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMutedBrush}" />
                            </Border>

                            <TextBlock Text="Status" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <TextBlock Text="{Binding StatusText}"
                                           Foreground="{StaticResource AccentBrush}" />
                            </Border>
                        </StackPanel>
                    </Grid>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Flight">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Flight Basics" Style="{StaticResource SectionTitleStyle}" Margin="0,16,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="{Binding Editor.ProfileName, UpdateSourceTrigger=LostFocus}"
                                     Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Frame rate" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="0" Grid.Column="3"
                                     Text="{Binding Editor.FrameRateHz, UpdateSourceTrigger=LostFocus}"
                                     Style="{StaticResource EditorTextBoxStyle}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Control mode" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox Grid.Row="1" Grid.Column="1"
                                      SelectedValue="{Binding Editor.ControlMode, Mode=TwoWay}"
                                      SelectedValuePath="Tag"
                                      Style="{StaticResource EditorComboBoxStyle}">
                                <ComboBoxItem Content="Direct mouse" Tag="direct_mouse" />
                                <ComboBoxItem Content="Reticle aim" Tag="drone_mouse_aim" />
                            </ComboBox>
                            <WrapPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2">
                                <CheckBox Content="Right stick enabled"
                                          IsChecked="{Binding Editor.MouseRightEnabled, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="2x resolution"
                                          IsChecked="{Binding Editor.HighResolution2xEnabled, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                            </WrapPanel>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Start/stop key" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="2" Grid.Column="1"
                                     Text="{Binding Editor.StopKey, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource EditorTextBoxStyle}"
                                     controls:HotkeyCapture.IsEnabled="True"
                                     ToolTip="Click, then press the key. Delete clears the value." />
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="Freeze key" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="2" Grid.Column="3"
                                     Text="{Binding Editor.FreezeKey, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource EditorTextBoxStyle}"
                                     controls:HotkeyCapture.IsEnabled="True"
                                     ToolTip="Click, then press the key. Delete clears the value." />
                        </Grid>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Right Stick Basic"
                                       Style="{StaticResource SectionTitleStyle}"
                                       Margin="0,6,0,8"
                                       Visibility="{Binding Editor.IsRightStickBasicActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <Border Style="{StaticResource RaisedBlockStyle}"
                                    Visibility="{Binding Editor.IsRightStickBasicActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Roll gain" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.RollGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Pitch gain" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.PitchGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Max output" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.MaxOutput, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Deadband" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Editor.Deadband, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Expo" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.Expo, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                                    <WrapPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,4,0,0">
                                        <CheckBox Content="Invert roll" IsChecked="{Binding Editor.InvertRoll, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Invert pitch" IsChecked="{Binding Editor.InvertPitch, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Swap axes" IsChecked="{Binding Editor.SwapAxes, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                    </WrapPanel>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Left Stick Source" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Source" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.LeftStickSource, Mode=TwoWay}"
                                                  SelectedValuePath="Content"
                                                  IsEnabled="{Binding Editor.IsDirectMouseMode}"
                                                  Style="{StaticResource EditorComboBoxStyle}">
                                            <ComboBoxItem Content="Off" />
                                            <ComboBoxItem Content="Keyboard / Wooting" />
                                            <ComboBoxItem Content="Second mouse" />
                                            <ComboBoxItem Content="Right Mouse Mouse4/Mouse5 + Scroll" />
                                        </ComboBox>
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsKeyboardLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Input" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.KeyboardInputSource, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}">
                                            <ComboBoxItem Content="GameInput" Tag="gameinput" />
                                            <ComboBoxItem Content="Wooting analog" Tag="wooting_analog" />
                                            <ComboBoxItem Content="Auto" Tag="auto" />
                                        </ComboBox>
                                        <CheckBox Grid.Column="2" Grid.ColumnSpan="2"
                                                  Content="Require analog"
                                                  IsChecked="{Binding Editor.KeyboardRequireAnalog, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <CheckBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                                                  Content="Require device"
                                                  IsChecked="{Binding Editor.MouseLeftRequireDevice, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Throttle" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.MouseLeftThrottleRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Yaw gain" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Editor.MouseLeftYawGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Yaw max" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.MouseLeftYawPulse, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="Yaw deadband" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Editor.MouseLeftYawDeadband, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <WrapPanel Visibility="{Binding Editor.IsMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,4,0,0">
                                        <CheckBox Content="Invert throttle" IsChecked="{Binding Editor.MouseLeftInvertThrottle, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Invert yaw" IsChecked="{Binding Editor.MouseLeftInvertYaw, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Swap axes" IsChecked="{Binding Editor.MouseLeftSwapAxes, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                    </WrapPanel>

                                    <Grid Visibility="{Binding Editor.IsRightMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Throttle step" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.RightMouseLeftThrottleStep, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Button rate" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.RightMouseLeftThrottleButtonRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Yaw max" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.RightMouseLeftYawPulse, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Yaw step" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Editor.RightMouseLeftYawScrollStep, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Yaw slew" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.RightMouseLeftYawSlewRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="Return" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Editor.RightMouseLeftThrottleReturnRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <WrapPanel Visibility="{Binding Editor.IsRightMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,4,0,0">
                                        <CheckBox Content="Throttle return" IsChecked="{Binding Editor.RightMouseLeftThrottleReturnEnabled, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Invert throttle" IsChecked="{Binding Editor.RightMouseLeftInvertThrottle, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Invert yaw" IsChecked="{Binding Editor.RightMouseLeftInvertYaw, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Swap buttons/scroll" IsChecked="{Binding Editor.RightMouseLeftSwapAxes, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Shaping">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Right Stick Shaping"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8"
                               Visibility="{Binding Editor.IsDirectMouseMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Style="{StaticResource RaisedBlockStyle}"
                            IsEnabled="{Binding Editor.IsDirectMouseMode}"
                            Visibility="{Binding Editor.IsDirectMouseMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <controls:StickShapeEditor NodesText="{Binding Editor.OutputShapeNodesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                               ShapingEnabled="{Binding Editor.IsOutputShapeActive}"
                                                               CurveBrush="{StaticResource InfoBrush}"
                                                               Height="142"
                                                               Hint="Linear"
                                                               Visibility="{Binding Editor.IsOutputShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                               ToolTip="Output shape nodes. Left-click adds or selects, drag moves, mouse wheel changes width, right-click removes." />
                                    <TextBlock Text="{Binding Editor.OutputShapeSummary}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,7,0,10"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{Binding Editor.IsOutputShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Text="Output Curve" Style="{StaticResource MetricLabelStyle}" Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="104" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Output curve" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.OutputCurve, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}"
                                                  ToolTip="Right-stick output curve mode. Shape nodes uses the curve editor above.">
                                            <ComboBoxItem Content="Expo" Tag="expo" />
                                            <ComboBoxItem Content="Shape nodes" Tag="nodes" />
                                            <ComboBoxItem Content="Actual rates" Tag="actual" />
                                        </ComboBox>
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsOutputCurveExpoSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="104" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Expo" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1"
                                                 Text="{Binding Editor.Expo, UpdateSourceTrigger=LostFocus}"
                                                 Style="{StaticResource EditorTextBoxStyle}"
                                                 ToolTip="Scalar expo used when output curve is Expo." />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsOutputCurveActualSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="104" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="104" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Actual center" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.ActualCenter, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Actual max" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.ActualMax, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Actual expo" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.ActualExpo, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <controls:StickShapeEditor NodesText="{Binding Editor.ReturnShapeNodesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                               ShapingEnabled="{Binding Editor.IsReturnShapeActive}"
                                                               CurveBrush="{StaticResource WarnBrush}"
                                                               Height="142"
                                                               Hint="Linear"
                                                               Visibility="{Binding Editor.IsReturnShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                               ToolTip="Return shape nodes. Left-click adds or selects, drag moves, mouse wheel changes width, right-click removes." />
                                    <TextBlock Text="{Binding Editor.ReturnShapeSummary}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,7,0,10"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{Binding Editor.IsReturnShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Text="Return" Style="{StaticResource MetricLabelStyle}" Margin="0,0,0,8" />
                                    <WrapPanel>
                                        <CheckBox Content="Idle return" IsChecked="{Binding Editor.ReturnEnabled, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Constant return" IsChecked="{Binding Editor.ConstantReturnEnabled, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Elastic return" IsChecked="{Binding Editor.ElasticReturnEnabled, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                        <CheckBox Content="Return shaping" IsChecked="{Binding Editor.ReturnShapingEnabled, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                    </WrapPanel>

                                    <Grid Visibility="{Binding Editor.IsIdleReturnActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Idle speed" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.ReturnRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Column="2" Text="Idle delay" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="3" Text="{Binding Editor.ReturnIdle, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsConstantReturnActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Constant speed" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.ConstantReturnRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsElasticReturnActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Elastic mode" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Row="0" Grid.Column="1"
                                                  SelectedValue="{Binding Editor.ElasticReturnMode, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}"
                                                  ToolTip="Elastic return curve family.">
                                            <ComboBoxItem Content="Progressive" Tag="progressive" />
                                            <ComboBoxItem Content="Linear" Tag="linear" />
                                            <ComboBoxItem Content="Smoothstep" Tag="smoothstep" />
                                            <ComboBoxItem Content="Expo" Tag="expo" />
                                        </ComboBox>
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Spring timing" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Row="0" Grid.Column="3"
                                                  SelectedValue="{Binding Editor.ElasticReturnActivation, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}"
                                                  ToolTip="Always keeps elastic return active on zero-input ticks. While moving uses Elastic coeff while mouse input is present, then fades toward Idle coeff after input stops.">
                                            <ComboBoxItem Content="Always" Tag="always" />
                                            <ComboBoxItem Content="While moving" Tag="while_moving" />
                                        </ComboBox>
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Return metric" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Row="1" Grid.Column="1"
                                                  SelectedValue="{Binding Editor.ElasticReturnMetric, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}"
                                                  ToolTip="Axis keeps independent-axis return. Diagonal normalized applies one 2D return vector so diagonals do not pull harder than cardinals.">
                                            <ComboBoxItem Content="Axis" Tag="axis" />
                                            <ComboBoxItem Content="Diagonal normalized" Tag="diagonal_normalized" />
                                        </ComboBox>
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Elastic coeff" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.ElasticReturnCoefficient, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="Elastic curve" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Editor.ElasticReturnCurve, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Idle coeff" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Editor.ElasticReturnIdleCoefficient, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="3" Grid.Column="2" Text="Taper ms" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding Editor.ElasticReturnTaperMs, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,18,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="Input Cleanup" Style="{StaticResource MetricLabelStyle}" Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Input filter" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.InputFilter, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}"
                                                  ToolTip="Pre-integrator input filtering.">
                                            <ComboBoxItem Content="Off" Tag="off" />
                                            <ComboBoxItem Content="Smoothing" Tag="smoothing" />
                                            <ComboBoxItem Content="One Euro" Tag="one_euro" />
                                        </ComboBox>
                                        <CheckBox Grid.Column="2" Grid.ColumnSpan="2"
                                                  Content="Mouse despike"
                                                  IsChecked="{Binding Editor.DespikeEnabled, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsInputFilterSmoothingSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Smoothing" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.Smoothing, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsInputFilterOneEuroSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="1-euro min" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.OneEuroMinCutoffHz, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="1-euro beta" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.OneEuroBeta, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="1-euro dcut" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.OneEuroDcutoffHz, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsDespikeActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Despike window" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.DespikeWindow, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Column="2" Text="Despike sigma" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="3" Text="{Binding Editor.DespikeThresholdSigma, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>
                                    <CheckBox Content="Count despikes"
                                              IsChecked="{Binding Editor.DespikeCountEnabled, Mode=TwoWay}"
                                              Style="{StaticResource EditorCheckBoxStyle}"
                                              Visibility="{Binding Editor.IsDespikeActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Model / Gain / Gate" Style="{StaticResource MetricLabelStyle}" Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Position model" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.PositionModel, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}">
                                            <ComboBoxItem Content="Integrator" Tag="integrator" />
                                            <ComboBoxItem Content="Dynamic gimbal" Tag="dynamic_gimbal" />
                                        </ComboBox>
                                        <TextBlock Grid.Column="2" Text="Gain mode" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="3"
                                                  SelectedValue="{Binding Editor.InputGainMode, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}">
                                            <ComboBoxItem Content="Flat" Tag="flat" />
                                            <ComboBoxItem Content="Adaptive" Tag="adaptive" />
                                        </ComboBox>
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsDynamicGimbalSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Gimbal Hz" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.GimbalFrequencyHz, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Damping" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.GimbalDampingRatio, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Impulse" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.GimbalInputImpulse, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Edge bumper" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Editor.GimbalEdgeBumper, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Static friction" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.GimbalStaticFriction, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="Dynamic friction" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Editor.GimbalDynamicFriction, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <CheckBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                                  Content="Gimbal anti-windup"
                                                  IsChecked="{Binding Editor.GimbalAntiwindupEnabled, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsGimbalAntiwindupParameterActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="AW start" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.GimbalAntiwindupStart, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                        <TextBlock Grid.Column="2" Text="AW min" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="3" Text="{Binding Editor.GimbalAntiwindupMinGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Radial gate" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox Grid.Column="1"
                                                  SelectedValue="{Binding Editor.GateShape, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource EditorComboBoxStyle}">
                                            <ComboBoxItem Content="Axis" Tag="axis" />
                                            <ComboBoxItem Content="Circle" Tag="circle" />
                                            <ComboBoxItem Content="Octagon" Tag="octagon" />
                                            <ComboBoxItem Content="Square" Tag="square" />
                                        </ComboBox>
                                        <TextBlock Grid.Column="2"
                                                   Text="Diagonal scale"
                                                   Style="{StaticResource FieldLabelStyle}"
                                                   Visibility="{Binding Editor.IsRadialGateSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <TextBox Grid.Column="3"
                                                 Text="{Binding Editor.DiagonalScale, UpdateSourceTrigger=LostFocus}"
                                                 Style="{StaticResource EditorTextBoxStyle}"
                                                 Visibility="{Binding Editor.IsRadialGateSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>

                                    <Grid Visibility="{Binding Editor.IsAdaptiveGainSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="118" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Adaptive tracker" Style="{StaticResource FieldLabelStyle}" />
                                        <TextBox Grid.Column="1" Text="{Binding Editor.AdaptiveTrackerMs, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Adaptive Gain"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8"
                               Visibility="{Binding Editor.IsAdaptiveGainSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Style="{StaticResource RaisedBlockStyle}"
                            IsEnabled="{Binding Editor.IsDirectMouseMode}"
                            Visibility="{Binding Editor.IsAdaptiveGainSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Slow gain" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Column="1" Text="{Binding Editor.AdaptiveSlowGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Column="2" Text="Fast gain" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Column="3" Text="{Binding Editor.AdaptiveFastGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Column="4" Text="Curve" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Column="5" Text="{Binding Editor.AdaptiveCurve, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                        </Grid>
                    </Border>

                    <TextBlock Text="Adaptive Speed"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8"
                               Visibility="{Binding Editor.IsAdaptiveGainSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Style="{StaticResource RaisedBlockStyle}"
                            IsEnabled="{Binding Editor.IsDirectMouseMode}"
                            Visibility="{Binding Editor.IsAdaptiveGainSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Low speed" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Column="1" Text="{Binding Editor.AdaptiveSpeedLow, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Column="2" Text="High speed" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Column="3" Text="{Binding Editor.AdaptiveSpeedHigh, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                        </Grid>
                    </Border>

                    <TextBlock Text="Left Yaw Shaping"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8"
                               Visibility="{Binding Editor.IsMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Style="{StaticResource RaisedBlockStyle}"
                            IsEnabled="{Binding Editor.IsMouseLeftSelected}"
                            Visibility="{Binding Editor.IsMouseLeftSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <controls:StickShapeEditor NodesText="{Binding Editor.MouseLeftYawOutputShapeNodesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                           ShapingEnabled="{Binding Editor.IsMouseLeftYawOutputShapeActive}"
                                                           CurveBrush="{StaticResource AccentBrush}"
                                                           Height="132"
                                                           Hint="Linear"
                                                           Visibility="{Binding Editor.IsMouseLeftYawOutputShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                           ToolTip="Second-mouse yaw output shape nodes. Left-click adds or selects, drag moves, mouse wheel changes width, right-click removes." />
                                <TextBlock Text="{Binding Editor.MouseLeftYawOutputShapeSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,7,0,10"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding Editor.IsMouseLeftYawOutputShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <WrapPanel Margin="0,0,0,8">
                                    <CheckBox Content="Yaw shaping"
                                              IsChecked="{Binding Editor.MouseLeftYawShapingEnabled, Mode=TwoWay}"
                                              Style="{StaticResource EditorCheckBoxStyle}" />
                                    <CheckBox Content="Output nodes"
                                              IsChecked="{Binding Editor.MouseLeftYawOutputShapingEnabled, Mode=TwoWay}"
                                              Style="{StaticResource EditorCheckBoxStyle}"
                                              Visibility="{Binding Editor.IsMouseLeftYawShapingActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </WrapPanel>
                                <Grid Margin="0,0,0,8"
                                      Visibility="{Binding Editor.IsMouseLeftYawShapingActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Yaw curve" Style="{StaticResource FieldLabelStyle}" />
                                    <ComboBox Grid.Column="1"
                                              SelectedValue="{Binding Editor.MouseLeftYawOutputCurve, Mode=TwoWay}"
                                              SelectedValuePath="Tag"
                                              Style="{StaticResource EditorComboBoxStyle}">
                                        <ComboBoxItem Content="Expo" Tag="expo" />
                                        <ComboBoxItem Content="Shape nodes" Tag="nodes" />
                                        <ComboBoxItem Content="Actual rates" Tag="actual" />
                                    </ComboBox>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <controls:StickShapeEditor NodesText="{Binding Editor.MouseLeftYawReturnShapeNodesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                           ShapingEnabled="{Binding Editor.IsMouseLeftYawReturnShapeActive}"
                                                           CurveBrush="{StaticResource DangerBrush}"
                                                           Height="132"
                                                           Hint="Linear"
                                                           Visibility="{Binding Editor.IsMouseLeftYawReturnShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                           ToolTip="Second-mouse yaw return shape nodes. Left-click adds or selects, drag moves, mouse wheel changes width, right-click removes." />
                                <TextBlock Text="{Binding Editor.MouseLeftYawReturnShapeSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="0,7,0,10"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding Editor.IsMouseLeftYawReturnShapeActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <CheckBox Content="Yaw return nodes"
                                          IsChecked="{Binding Editor.MouseLeftYawReturnShapingEnabled, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}"
                                          Margin="0,0,0,8"
                                          Visibility="{Binding Editor.IsMouseLeftYawShapingActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </Grid>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Devices">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Devices" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="118" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="118" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Right token" Style="{StaticResource FieldLabelStyle}" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding SelectedProfile.RightDeviceDisplay}"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Text="Left token" Style="{StaticResource FieldLabelStyle}" />
                                <TextBlock Grid.Column="3"
                                           Text="{Binding SelectedProfile.LeftDeviceDisplay}"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="76" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="GameInput enumeration"
                                           Style="{StaticResource FieldLabelStyle}"
                                           VerticalAlignment="Center" />
                                <TextBox Grid.Column="1"
                                         Text="{Binding Editor.DeviceScanSeconds, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource EditorTextBoxStyle}"
                                         MinWidth="54"
                                         Margin="0,0,8,0"
                                         ToolTip="Duration in seconds, 1..30." />
                                <Button Grid.Column="2"
                                        Content="Run"
                                        Command="{Binding Editor.RunMouseDevicesCommand}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <Button Grid.Column="3"
                                        Content="Copy"
                                        Command="{Binding Editor.CopyCommandLineCommand}"
                                        CommandParameter="{Binding Editor.MouseDevicesCommandLine}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <TextBox Grid.Column="4"
                                         Text="{Binding Editor.MouseDevicesCommandLine, Mode=OneWay}"
                                         IsReadOnly="True"
                                         FontFamily="Consolas"
                                         FontSize="11"
                                         TextWrapping="NoWrap"
                                         HorizontalScrollBarVisibility="Auto" />
                            </Grid>
                            <TextBlock Grid.Row="2"
                                       Text="Move one physical mouse at a time while the scanner is running; use the reported token in mouse_devices.right or mouse_devices.left."
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,0,0,8" />
                            <DataGrid Grid.Row="3"
                                      ItemsSource="{Binding Editor.DeviceAssignmentStatuses}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      Height="76"
                                      Margin="0,0,0,8">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Role"
                                                        Binding="{Binding Role}"
                                                        Width="66" />
                                    <DataGridTextColumn Header="Token"
                                                        Binding="{Binding TokenDisplay}"
                                                        Width="110" />
                                    <DataGridTextColumn Header="Status"
                                                        Binding="{Binding StatusLabel}"
                                                        Width="74" />
                                    <DataGridTextColumn Header="Assignment check"
                                                        Binding="{Binding Message}"
                                                        Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock Grid.Row="4"
                                       Text="{Binding Editor.DeviceAssignmentSummary}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,0,0,8" />
                            <DataGrid Grid.Row="5"
                                      ItemsSource="{Binding Editor.GameInputDevices}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      Height="124">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Root"
                                                        Binding="{Binding RootDisplay}"
                                                        Width="116" />
                                    <DataGridTextColumn Header="VID/PID"
                                                        Binding="{Binding VendorProductDisplay}"
                                                        Width="116" />
                                    <DataGridTextColumn Header="Movement"
                                                        Binding="{Binding MovementSummary}"
                                                        Width="138" />
                                    <DataGridTextColumn Header="Name"
                                                        Binding="{Binding Name}"
                                                        Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Recordings">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.05*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Capture" Style="{StaticResource SectionTitleStyle}" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="132" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Grid.Column="0"
                                               Text="Recording file"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="0"
                                             Grid.Column="1"
                                             Text="{Binding RecordingPath, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             FontFamily="Consolas"
                                             HorizontalScrollBarVisibility="Auto"
                                             TextWrapping="NoWrap"
                                             ToolTip="Relative paths are resolved from the app folder." />
                                    <Button Grid.Row="0"
                                            Grid.Column="2"
                                            Content="Save as"
                                            Command="{Binding ChooseRecordingPathCommand}"
                                            Style="{StaticResource CompactButtonStyle}"
                                            Margin="8,0,0,6"
                                            ToolTip="Choose the recording folder and file name." />

                                    <TextBlock Grid.Row="1"
                                               Grid.Column="0"
                                               Text="Duration"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1"
                                             Grid.Column="1"
                                             Text="{Binding RecordingDurationSeconds, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             MinWidth="72"
                                             ToolTip="Timed capture length in seconds, 1..3600." />
                                    <CheckBox Grid.Row="1"
                                              Grid.Column="2"
                                              Grid.ColumnSpan="2"
                                              Content="Live reload"
                                              IsChecked="{Binding RecordingLiveReload, Mode=TwoWay}"
                                              Style="{StaticResource EditorCheckBoxStyle}"
                                              ToolTip="Pass live to gx12mouse while recording." />

                                    <TextBlock Grid.Row="2"
                                               Grid.Column="0"
                                               Text="Record key"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="2"
                                             Grid.Column="1"
                                             Text="{Binding RecordingToggleKey, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             MinWidth="72"
                                             controls:HotkeyCapture.IsEnabled="True"
                                             controls:HotkeyCapture.ClearValue="off"
                                             ToolTip="Click, then press the key or mouse button. Delete sets off for immediate timed recording." />
                                    <CheckBox Grid.Row="2"
                                              Grid.Column="2"
                                              Grid.ColumnSpan="2"
                                              Content="Overwrite file"
                                              IsChecked="{Binding RecordingOverwrite, Mode=TwoWay}"
                                              Style="{StaticResource EditorCheckBoxStyle}"
                                              ToolTip="Record to the selected file name instead of creating numbered clips." />

                                    <WrapPanel Grid.Row="3"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="4"
                                               Margin="0,2,0,4">
                                        <Button Content="Copy record"
                                                Command="{Binding CopyRecordingCommandLineCommand}"
                                                Style="{StaticResource CompactButtonStyle}"
                                                Margin="0,0,8,6"
                                                ToolTip="{Binding RecordingCommandLine}" />
                                        <Button Content="{Binding RecordingInfoButtonText}"
                                                Command="{Binding InspectRecordingCommand}"
                                                Style="{StaticResource CompactButtonStyle}"
                                                Margin="0,0,8,6"
                                                ToolTip="{Binding RecordingInfoCommandLine}" />
                                        <Button Content="Copy info"
                                                Command="{Binding CopyRecordingInfoCommandLineCommand}"
                                                Style="{StaticResource CompactButtonStyle}"
                                                Margin="0,0,8,6"
                                                ToolTip="{Binding RecordingInfoCommandLine}" />
                                    </WrapPanel>

                                    <TextBlock Grid.Row="4"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="4"
                                               Text="{Binding RecordingBufferStatusText}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,0,0,6" />

                                    <TextBox Grid.Row="5"
                                             Grid.Column="0"
                                             Grid.ColumnSpan="4"
                                             Text="{Binding RecordingCommandLine, Mode=OneWay}"
                                             IsReadOnly="True"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             Height="58"
                                             TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             ToolTip="Copyable gx12mouse --trainer-record command." />
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Playback" Style="{StaticResource SectionTitleStyle}" />
                            <Border Style="{StaticResource RaisedBlockStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="96" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="86" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                     <Grid.RowDefinitions>
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                         <RowDefinition Height="Auto" />
                                     </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Grid.Column="0"
                                               Text="File"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="0"
                                             Grid.Column="1"
                                             Grid.ColumnSpan="2"
                                             Text="{Binding PlaybackRecordingPath, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             FontFamily="Consolas"
                                             HorizontalScrollBarVisibility="Auto"
                                             TextWrapping="NoWrap"
                                             ToolTip="Recording file to play back." />
                                    <Button Grid.Row="0"
                                            Grid.Column="3"
                                            Content="Open"
                                            Command="{Binding ChoosePlaybackPathCommand}"
                                            Style="{StaticResource CompactButtonStyle}"
                                            Margin="8,0,0,6"
                                            HorizontalAlignment="Right"
                                            ToolTip="Choose a recording file to play back." />

                                    <TextBlock Grid.Row="1"
                                               Grid.Column="0"
                                               Text="Port"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1"
                                             Grid.Column="1"
                                             Text="{Binding PlaybackPort, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             ToolTip="Use auto or a COM port such as COM6." />
                                    <TextBlock Grid.Row="1"
                                               Grid.Column="2"
                                               Text="Trigger"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1"
                                             Grid.Column="3"
                                             Text="{Binding PlaybackTrigger, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource EditorTextBoxStyle}"
                                             controls:HotkeyCapture.IsEnabled="True"
                                             controls:HotkeyCapture.ClearValue="off"
                                             ToolTip="Click, then press the key or mouse button. Delete disables the inline playback bind." />

                                    <TextBlock Grid.Row="2"
                                               Grid.Column="0"
                                               Text="Channels"
                                               Style="{StaticResource FieldLabelStyle}" />
                                    <WrapPanel Grid.Row="2"
                                               Grid.Column="1"
                                               Grid.ColumnSpan="3">
                                        <CheckBox Content="Ail"
                                                  IsChecked="{Binding PlaybackAileron, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Replay recorded aileron on channel 1." />
                                        <CheckBox Content="Ele"
                                                  IsChecked="{Binding PlaybackElevator, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Replay recorded elevator on channel 2." />
                                        <CheckBox Content="Thr"
                                                  IsChecked="{Binding PlaybackThrottle, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Replay recorded throttle on channel 3." />
                                        <CheckBox Content="Rud"
                                                  IsChecked="{Binding PlaybackRudder, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Replay recorded rudder on channel 4." />
                                        <TextBlock Text="{Binding PlaybackChannelMask}"
                                                   Style="{StaticResource MutedTextStyle}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,0,8" />
                                    </WrapPanel>

                                    <WrapPanel Grid.Row="3"
                                               Grid.Column="1"
                                               Grid.ColumnSpan="2">
                                        <CheckBox Content="Loop"
                                                  IsChecked="{Binding PlaybackLoop, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Repeat the recording until stopped." />
                                        <CheckBox Content="Radio right gimbal"
                                                  IsChecked="{Binding PlaybackRadioRightGimbal, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Use recorded GX12 HID ch1/ch2 for Ail/Ele instead of the PC mouse track." />
                                        <CheckBox Content="Recorded trainer right"
                                                  IsChecked="{Binding PlaybackRecordedTrainerRight, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Use the recorded final trainer ch1/ch2 values instead of re-running the PC mouse mapper." />
                                        <CheckBox Content="Radio left gimbal"
                                                  IsChecked="{Binding PlaybackRadioLeftGimbal, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Use recorded GX12 HID ch3/ch4 for Thr/Rud instead of the PC left-source track." />
                                        <CheckBox Content="Recorded trainer left"
                                                  IsChecked="{Binding PlaybackRecordedTrainerLeft, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Use the recorded final trainer ch3/ch4 values instead of re-running the PC left-source mapper." />
                                        <CheckBox Content="Block live input"
                                                  IsChecked="{Binding PlaybackBlockLiveInput, Mode=TwoWay}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Ignore live input on the channels this playback bind controls while it is active." />
                                    </WrapPanel>
                                    <WrapPanel Grid.Row="3"
                                               Grid.Column="3"
                                               HorizontalAlignment="Right">
                                        <Button Content="Copy playback"
                                                Command="{Binding CopyPlaybackCommandLineCommand}"
                                                Style="{StaticResource CompactButtonStyle}"
                                                Margin="0,0,0,6"
                                                ToolTip="{Binding PlaybackCommandLine}" />
                                    </WrapPanel>

                                    <TextBlock Grid.Row="4"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="4"
                                               Text="{Binding RecordingBufferStatusText}"
                                               Style="{StaticResource MutedTextStyle}"
                                               Margin="0,0,0,6" />

                                    <TextBox Grid.Row="5"
                                             Grid.Column="0"
                                             Grid.ColumnSpan="4"
                                             Text="{Binding PlaybackCommandLine, Mode=OneWay}"
                                             IsReadOnly="True"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             Height="58"
                                             TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             ToolTip="Copyable gx12mouse --trainer-profile command with this playback bind." />

                                    <Grid Grid.Row="6"
                                          Grid.Column="0"
                                          Grid.ColumnSpan="4"
                                          Margin="0,10,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="Playback binds"
                                                   Style="{StaticResource SectionTitleStyle}"
                                                   Margin="0,0,0,0" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding PlaybackBindingLimitText}"
                                                   Style="{StaticResource MutedTextStyle}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,10,0" />
                                        <CheckBox Grid.Column="2"
                                                  Content="Add bind"
                                                  IsChecked="{Binding AddPlaybackBindingToggle, Mode=TwoWay}"
                                                  IsEnabled="{Binding CanAddPlaybackBinding}"
                                                  Style="{StaticResource EditorCheckBoxStyle}"
                                                  ToolTip="Adds another playback hotkey row, up to 12." />
                                    </Grid>

                                    <Grid Grid.Row="6"
                                          Grid.Column="0"
                                          Grid.ColumnSpan="4"
                                          Margin="0,0,0,4">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0" Margin="0,0,0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="76" />
                                                <ColumnDefinition Width="70" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="76" />
                                                <ColumnDefinition Width="96" />
                                                <ColumnDefinition Width="86" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="On" Style="{StaticResource MetricLabelStyle}" />
                                            <TextBlock Grid.Column="1" Text="Key" Style="{StaticResource MetricLabelStyle}" />
                                            <TextBlock Grid.Column="2" Text="Recording" Style="{StaticResource MetricLabelStyle}" />
                                            <TextBlock Grid.Column="4" Text="Channels" Style="{StaticResource MetricLabelStyle}" />
                                            <TextBlock Grid.Column="5" Text="Live" Style="{StaticResource MetricLabelStyle}" />
                                        </Grid>
                                        <ScrollViewer Grid.Row="1"
                                                      MaxHeight="178"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding PlaybackBindings}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,5">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="76" />
                                                                <ColumnDefinition Width="70" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="76" />
                                                                <ColumnDefinition Width="96" />
                                                                <ColumnDefinition Width="86" />
                                                            </Grid.ColumnDefinitions>
                                                            <CheckBox Grid.Column="0"
                                                                      Content="{Binding SlotNumber, StringFormat=Bind {0}}"
                                                                      IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                      Style="{StaticResource EditorCheckBoxStyle}"
                                                                      ToolTip="Turn this bind off to remove it from the playback bank." />
                                                            <TextBox Grid.Column="1"
                                                                     Text="{Binding Trigger, UpdateSourceTrigger=PropertyChanged}"
                                                                     Style="{StaticResource EditorTextBoxStyle}"
                                                                     Margin="0,0,6,4"
                                                                     controls:HotkeyCapture.IsEnabled="True"
                                                                     ToolTip="Click, then press the key or mouse button for this playback file." />
                                                            <TextBox Grid.Column="2"
                                                                     Text="{Binding RecordingPath, UpdateSourceTrigger=PropertyChanged}"
                                                                     Style="{StaticResource EditorTextBoxStyle}"
                                                                     FontFamily="Consolas"
                                                                     HorizontalScrollBarVisibility="Auto"
                                                                     TextWrapping="NoWrap"
                                                                     Margin="0,0,6,4"
                                                                     ToolTip="Recording file for this playback hotkey. Relative paths resolve from the app folder." />
                                                            <Button Grid.Column="3"
                                                                    Content="Open"
                                                                    Command="{Binding DataContext.ChoosePlaybackBindingPathCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                                    CommandParameter="{Binding}"
                                                                    Style="{StaticResource CompactButtonStyle}"
                                                                    Margin="0,0,6,4"
                                                                    ToolTip="Choose the recording file for this playback hotkey." />
                                                            <TextBox Grid.Column="4"
                                                                     Text="{Binding ChannelMask, UpdateSourceTrigger=PropertyChanged}"
                                                                     Style="{StaticResource EditorTextBoxStyle}"
                                                                     FontFamily="Consolas"
                                                                     Margin="0,0,0,4"
                                                                     ToolTip="Channels for this bind, for example ail,ele,thr,rud or radio_ail,radio_ele,radio_thr,radio_rud." />
                                                            <CheckBox Grid.Column="5"
                                                                      Content="Block"
                                                                      IsChecked="{Binding BlockLiveInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                      Style="{StaticResource EditorCheckBoxStyle}"
                                                                      Margin="8,0,0,4"
                                                                      ToolTip="Ignore live input on this bind's selected playback channels until the clip stops." />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>

                                    <WrapPanel Grid.Row="7"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="4"
                                               HorizontalAlignment="Right">
                                        <Button Content="Copy bank"
                                                Command="{Binding CopyPlaybackBankCommandLineCommand}"
                                                Style="{StaticResource CompactButtonStyle}"
                                                Margin="0,0,0,6"
                                                ToolTip="{Binding PlaybackBankCommandLine}" />
                                    </WrapPanel>

                                    <TextBox Grid.Row="8"
                                             Grid.Column="0"
                                             Grid.ColumnSpan="4"
                                             Text="{Binding PlaybackBankCommandLine, Mode=OneWay}"
                                             IsReadOnly="True"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             Height="58"
                                             TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             ToolTip="Copyable gx12mouse --trainer-playback-bank command." />

                                    <TextBlock Grid.Row="9"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="4"
                                               Text="{Binding RecordingStatusText}"
                                               Style="{StaticResource MutedTextStyle}"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,8,0,0" />
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Recording Info" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <TextBox Text="{Binding RecordingInfoText, Mode=OneWay}"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 FontFamily="Consolas"
                                 FontSize="11"
                                 Height="220"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 TextWrapping="NoWrap" />
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Diagnostics">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Diagnostics" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="142" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Profile validation" Style="{StaticResource FieldLabelStyle}" />
                                <Button Grid.Column="1"
                                        Content="Run"
                                        Command="{Binding Editor.RunShowProfileCommand}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <Button Grid.Column="2"
                                        Content="Copy"
                                        Command="{Binding Editor.CopyCommandLineCommand}"
                                        CommandParameter="{Binding Editor.ShowProfileCommandLine}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <TextBox Grid.Column="3"
                                         Text="{Binding Editor.ShowProfileCommandLine, Mode=OneWay}"
                                         IsReadOnly="True"
                                         FontFamily="Consolas"
                                         FontSize="11"
                                         TextWrapping="NoWrap"
                                         HorizontalScrollBarVisibility="Auto" />
                            </Grid>

                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="142" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Gimbal preview" Style="{StaticResource FieldLabelStyle}" />
                                <Button Grid.Column="1"
                                        Content="Run"
                                        Command="{Binding Editor.RunGimbalPreviewCommand}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <Button Grid.Column="2"
                                        Content="Copy"
                                        Command="{Binding Editor.CopyCommandLineCommand}"
                                        CommandParameter="{Binding Editor.GimbalPreviewCommandLine}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <TextBox Grid.Column="3"
                                         Text="{Binding Editor.GimbalPreviewCommandLine, Mode=OneWay}"
                                         IsReadOnly="True"
                                         FontFamily="Consolas"
                                         FontSize="11"
                                         TextWrapping="NoWrap"
                                         HorizontalScrollBarVisibility="Auto" />
                            </Grid>

                            <Grid Grid.Row="2" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="142" />
                                    <ColumnDefinition Width="76" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Second mouse dry run" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Grid.Column="1"
                                         Text="{Binding Editor.MouseLeftDryRunSeconds, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource EditorTextBoxStyle}"
                                         MinWidth="54"
                                         Margin="0,0,8,0"
                                         ToolTip="Duration in seconds, 1..60." />
                                <Button Grid.Column="2"
                                        Content="Run"
                                        Command="{Binding Editor.RunMouseLeftDryRunCommand}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <Button Grid.Column="3"
                                        Content="Copy"
                                        Command="{Binding Editor.CopyCommandLineCommand}"
                                        CommandParameter="{Binding Editor.MouseLeftDryRunCommandLine}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <TextBox Grid.Column="4"
                                         Text="{Binding Editor.MouseLeftDryRunCommandLine, Mode=OneWay}"
                                         IsReadOnly="True"
                                         FontFamily="Consolas"
                                         FontSize="11"
                                         TextWrapping="NoWrap"
                                         HorizontalScrollBarVisibility="Auto" />
                            </Grid>

                            <Grid Grid.Row="3" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Editor.DiagnosticStatus}"
                                           Style="{StaticResource MutedTextStyle}"
                                           VerticalAlignment="Center"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis" />
                                <Button Grid.Column="1"
                                        Content="Stop"
                                        Command="{Binding Editor.StopDiagnosticCommand}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="12,6"
                                        Margin="0,0,8,0" />
                                <Button Grid.Column="2"
                                        Content="Copy output"
                                        Command="{Binding Editor.CopyDiagnosticOutputCommand}"
                                        Padding="12,6" />
                            </Grid>

                            <TextBox Grid.Row="4"
                                     Text="{Binding Editor.DiagnosticText, Mode=OneWay}"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     AcceptsTab="True"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Height="190"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     TextWrapping="NoWrap" />

                            <Grid Grid.Row="5" Margin="0,10,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Editor.DiagnosticHistorySummary}"
                                               Style="{StaticResource MutedTextStyle}"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Editor.DiagnosticLogPathDisplay}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Content="Copy log path"
                                        Command="{Binding Editor.CopyDiagnosticLogPathCommand}"
                                        Padding="12,6"
                                        Margin="10,0,0,0" />
                            </Grid>

                            <DataGrid Grid.Row="6"
                                      ItemsSource="{Binding Editor.DiagnosticHistory}"
                                      SelectedItem="{Binding Editor.SelectedDiagnosticHistoryItem, Mode=TwoWay}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      Height="118">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Time"
                                                        Binding="{Binding CompletedAtText}"
                                                        Width="92" />
                                    <DataGridTextColumn Header="Diagnostic"
                                                        Binding="{Binding Name}"
                                                        Width="138" />
                                    <DataGridTextColumn Header="Status"
                                                        Binding="{Binding StatusLabel}"
                                                        Width="62" />
                                    <DataGridTextColumn Header="Duration"
                                                        Binding="{Binding DurationText}"
                                                        Width="72" />
                                    <DataGridTextColumn Header="Preview"
                                                        Binding="{Binding OutputPreview}"
                                                        Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Reticle Aim">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Reticle Aim Mode"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <CheckBox Content="Enable reticle aim"
                                  IsChecked="{Binding Editor.MouseAimEnabled, Mode=TwoWay}"
                                  Style="{StaticResource EditorCheckBoxStyle}" />
                    </Border>

                    <TextBlock Text="Reticle Aim Basic"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,6,0,8"
                               Visibility="{Binding Editor.IsMouseAimMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Style="{StaticResource RaisedBlockStyle}"
                            IsEnabled="{Binding Editor.IsMouseAimMode}"
                            Visibility="{Binding Editor.IsMouseAimMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="118" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Sensitivity X" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Editor.AimSensitivityX, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Sensitivity Y" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Editor.AimSensitivityY, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="0" Grid.Column="4" Text="Slew rate" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="0" Grid.Column="5" Text="{Binding Editor.AimSlewRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Reticle limit" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Editor.AimReticleLimit, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Deadband" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Editor.AimReticleDeadband, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="1" Grid.Column="4" Text="Return" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="1" Grid.Column="5" Text="{Binding Editor.AimReturnRate, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Roll gain" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Editor.AimRollGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="Yaw gain" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Editor.AimYawGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="2" Grid.Column="4" Text="Pitch gain" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="2" Grid.Column="5" Text="{Binding Editor.AimPitchGain, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Roll max" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Editor.AimRollMax, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="Yaw max" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding Editor.AimYawMax, UpdateSourceTrigger=LostFocus}" Style="{StaticResource EditorTextBoxStyle}" />
                            <WrapPanel Grid.Row="3" Grid.Column="4" Grid.ColumnSpan="2">
                                <CheckBox Content="Invert X" IsChecked="{Binding Editor.AimInvertX, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="Invert Y" IsChecked="{Binding Editor.AimInvertY, Mode=TwoWay}" Style="{StaticResource EditorCheckBoxStyle}" />
                            </WrapPanel>
                        </Grid>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Catalog">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="Settings Catalog" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding Editor.CatalogSearchText, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Search labels, TOML key names, groups, pages, and current values." />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Editor.CatalogSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0" />
                            </Grid>

                            <WrapPanel Margin="0,0,0,10">
                                <CheckBox Content="Basic"
                                          IsChecked="{Binding Editor.ShowBasicSettings, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="Advanced"
                                          IsChecked="{Binding Editor.ShowAdvancedSettings, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="Tuning"
                                          IsChecked="{Binding Editor.ShowExperimentalSettings, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="Changed only"
                                          IsChecked="{Binding Editor.ShowChangedSettingsOnly, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                                <CheckBox Content="Invalid only"
                                          IsChecked="{Binding Editor.ShowInvalidSettingsOnly, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}" />
                            </WrapPanel>

                            <DataGrid ItemsSource="{Binding Editor.CatalogRowsView}"
                                      SelectedItem="{Binding Editor.SelectedCatalogRow, Mode=TwoWay}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      Height="420">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tier"
                                                        Binding="{Binding TierLabel}"
                                                        Width="86" />
                                    <DataGridTextColumn Header="Group"
                                                        Binding="{Binding Group}"
                                                        Width="160" />
                                    <DataGridTemplateColumn Header="Setting" Width="170">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Label}"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap"
                                                           ToolTipService.ShowDuration="30000">
                                                    <TextBlock.ToolTip>
                                                        <ToolTip Background="{StaticResource BgRaisedBrush}"
                                                                 BorderBrush="{StaticResource AccentBrush}"
                                                                 BorderThickness="1"
                                                                 DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
                                                                 MaxWidth="300">
                                                            <StackPanel Margin="11">
                                                                <TextBlock Text="{Binding Label}"
                                                                           FontWeight="SemiBold"
                                                                           FontSize="13"
                                                                           Margin="0,0,0,5" />
                                                                <TextBlock Text="{Binding Help}"
                                                                           Foreground="{StaticResource TextBrush}"
                                                                           TextWrapping="Wrap"
                                                                           Margin="0,0,0,6" />
                                                                <TextBlock Text="{Binding DetailText}"
                                                                           Foreground="{StaticResource TextMutedBrush}"
                                                                           Visibility="{Binding HasDetailText, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                           Margin="0,0,0,6" />
                                                                <TextBlock Text="{Binding RiskText}"
                                                                           Foreground="{StaticResource WarnBrush}"
                                                                           Visibility="{Binding HasRiskText, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                           Margin="0,0,0,6" />
                                                                <TextBlock Text="{Binding TomlPath}"
                                                                           FontFamily="Consolas"
                                                                           FontSize="11"
                                                                           Foreground="{StaticResource InfoBrush}"
                                                                           Margin="0,0,0,7" />
                                                                <Image Source="{Binding TooltipImagePath}"
                                                                       MaxWidth="180"
                                                                       MaxHeight="140"
                                                                       Stretch="Uniform"
                                                                       Visibility="{Binding HasTooltipImage, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                       Margin="0,0,0,7" />
                                                                <TextBlock Text="{Binding TooltipImageStatus}"
                                                                           Foreground="{StaticResource TextMutedBrush}"
                                                                           FontSize="11"
                                                                           TextWrapping="Wrap" />
                                                            </StackPanel>
                                                        </ToolTip>
                                                    </TextBlock.ToolTip>
                                                </TextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="TOML"
                                                        Binding="{Binding TomlPath}"
                                                        Width="190" />
                                    <DataGridTextColumn Header="Value"
                                                        Binding="{Binding DisplayValue}"
                                                        Width="120" />
                                    <DataGridTextColumn Header="Default"
                                                        Binding="{Binding DefaultDisplayValue}"
                                                        Width="105" />
                                    <DataGridTextColumn Header="Status"
                                                        Binding="{Binding StatusLabel}"
                                                        Width="84" />
                                    <DataGridTextColumn Header="Image"
                                                        Binding="{Binding TooltipImageBadge}"
                                                        Width="64" />
                                    <DataGridTemplateColumn Header="Jump" Width="72">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Go"
                                                        Padding="7,3"
                                                        MinHeight="0"
                                                        Command="{Binding DataContext.Editor.JumpToCatalogRowCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                        CommandParameter="{Binding}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <TextBlock Text="{Binding Editor.CatalogJumpText}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,8,0,10" />

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="Selected Profile TOML" Style="{StaticResource SectionTitleStyle}" />
                                    <TextBox Text="{Binding Editor.RawTomlText, Mode=OneWay}"
                                             IsReadOnly="True"
                                             AcceptsReturn="True"
                                             AcceptsTab="True"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             Height="220"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             TextWrapping="NoWrap" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Catalog Detail" Style="{StaticResource SectionTitleStyle}" />
                                    <Border Background="#181D20"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="10"
                                            MinHeight="220">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Editor.SelectedCatalogLabel}"
                                                       FontSize="15"
                                                       FontWeight="SemiBold"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Editor.CatalogJumpText}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       Margin="0,5,0,8" />
                                            <TextBlock Text="{Binding Editor.SelectedCatalogHelp}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,8" />
                                            <Grid IsEnabled="{Binding Editor.IsCatalogEditEnabled}"
                                                  Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Grid.Column="0"
                                                         Text="{Binding Editor.CatalogEditValue, UpdateSourceTrigger=PropertyChanged}"
                                                         ToolTip="Edit the selected setting value. String and enum values are saved as TOML strings automatically." />
                                                <Button Grid.Column="1"
                                                        Content="Apply"
                                                        Command="{Binding Editor.ApplyCatalogValueCommand}"
                                                        Padding="12,6"
                                                        Margin="8,0,0,0" />
                                                <Button Grid.Column="2"
                                                        Content="Default"
                                                        Command="{Binding Editor.ResetCatalogValueCommand}"
                                                        Padding="12,6"
                                                        Margin="8,0,0,0" />
                                            </Grid>
                                            <TextBlock Text="{Binding Editor.SelectedCatalogDetail}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       Margin="0,0,0,5" />
                                            <TextBlock Text="{Binding Editor.SelectedCatalogAllowedValues}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       Margin="0,0,0,5" />
                                            <TextBlock Text="{Binding Editor.SelectedCatalogRawValue, StringFormat=Raw: {0}}"
                                                       FontFamily="Consolas"
                                                       FontSize="11"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,0,0,8" />
                                            <TextBlock Text="{Binding Editor.TooltipImageStatus}"
                                                       Foreground="{StaticResource WarnBrush}"
                                                       FontSize="12"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,8" />
                                            <Border Background="#101416"
                                                    BorderBrush="{StaticResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="6"
                                                    Padding="8"
                                                    HorizontalAlignment="Left"
                                                    Visibility="{Binding Editor.HasSelectedTooltipImage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <Image Source="{Binding Editor.SelectedTooltipImagePath}"
                                                       MaxWidth="180"
                                                       MaxHeight="140"
                                                       Stretch="Uniform" />
                                            </Border>
                                            <TextBlock Text="{Binding Editor.SelectedTooltipImageDisplayPath}"
                                                       FontFamily="Consolas"
                                                       FontSize="11"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,8,0,0" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Files">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,14,0,0">

                    <TextBlock Text="App Settings" Style="{StaticResource SectionTitleStyle}" Margin="0,6,0,8" />
                    <Border Style="{StaticResource RaisedBlockStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="UI settings" Style="{StaticResource FieldLabelStyle}" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4"
                                       Text="{Binding Editor.UiSettingsFilePath}"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Setting tooltip images" Style="{StaticResource FieldLabelStyle}" Margin="0,8,8,0" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4"
                                       Text="{Binding Editor.TooltipImageDirectory}"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,8,0,0" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Random sprites" Style="{StaticResource FieldLabelStyle}" Margin="0,8,8,0" />
                            <TextBlock Grid.Row="2" Grid.Column="1"
                                       Text="{Binding Editor.TooltipSpriteDirectory}"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,8,0,0" />
                            <Button Grid.Row="2" Grid.Column="2"
                                    Content="Copy folder"
                                    Command="{Binding Editor.CopyTooltipSpriteDirectoryCommand}"
                                    Padding="12,6"
                                    Margin="10,8,0,0" />
                            <Button Grid.Row="2" Grid.Column="3"
                                    Content="Refresh"
                                    Command="{Binding Editor.RefreshTooltipSpritesCommand}"
                                    Padding="12,6"
                                    Margin="8,8,0,0" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Asuka sprite" Style="{StaticResource FieldLabelStyle}" Margin="0,8,8,0" />
                            <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,8,0,0">
                                <CheckBox Content="Random return"
                                          IsChecked="{Binding Editor.AboveBarSpriteRandomReturnDelay, Mode=TwoWay}"
                                          Style="{StaticResource EditorCheckBoxStyle}"
                                          ToolTip="Controls whether the Asuka sprite return delay is random or fixed." />
                                <TextBlock Text="Fixed seconds"
                                           Style="{StaticResource FieldLabelStyle}"
                                           Margin="4,0,8,8" />
                                <TextBox Text="{Binding Editor.AboveBarSpriteFixedReturnDelaySecondsText, UpdateSourceTrigger=LostFocus}"
                                         Style="{StaticResource EditorTextBoxStyle}"
                                         Width="88"
                                         IsEnabled="{Binding Editor.IsAboveBarSpriteFixedReturnDelayEnabled}"
                                         ToolTip="Seconds to wait before the Asuka sprite returns when random return is off." />
                                <TextBlock Text="{Binding Editor.AboveBarSpriteReturnSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           Margin="2,2,0,8"
                                           VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,8,0">
                                <TextBlock
                                       Text="{Binding Editor.TooltipSettingsSummary}"
                                       Style="{StaticResource MutedTextStyle}" />
                                <TextBlock
                                       Text="{Binding Editor.TooltipSpriteSummary}"
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,2,0,0" />
                            </StackPanel>
                            <Button Grid.Row="4" Grid.Column="2"
                                    Content="Attach image"
                                    Command="{Binding Editor.ChooseTooltipImageCommand}"
                                    Padding="12,6"
                                    Margin="10,8,0,0" />
                            <Button Grid.Row="4" Grid.Column="3"
                                    Content="Clear"
                                    Command="{Binding Editor.ClearTooltipImageCommand}"
                                    Padding="12,6"
                                    Margin="8,8,0,0" />
                            <Button Grid.Row="4" Grid.Column="4"
                                    Content="Copy path"
                                    Command="{Binding Editor.CopyTooltipImagePathCommand}"
                                    Padding="12,6"
                                    Margin="8,8,0,0" />
                        </Grid>
                    </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </DockPanel>
        </Border>
    </Grid>
</Window>
